# Security Policy

Do not report credentials, IBANs, access tokens or other secrets in public GitHub Issues.

For security vulnerabilities, use GitHub's private vulnerability reporting/security advisory mechanism when enabled, or contact the repository maintainers through a private channel.

Security-sensitive configuration must be supplied through environment variables or a secret manager and must never be committed.
