# Contributing to ISHBounty

1. Open or select a GitHub Issue.
2. For bounty work, read its acceptance criteria and verification requirements.
3. Create a branch and submit a Pull Request referencing the issue (`Fixes #N` when appropriate).
4. Keep changes focused and include tests for behavior changes.
5. CI must pass before merge.

Do not commit secrets, private payment information, generated credentials or user data.
